# WireGuard Tools

Два скрипта для быстрого развёртывания WireGuard — сервер и исходящий клиент.

## 📦 Что внутри

| Скрипт | Назначение |
|--------|-----------|
| `wg-server.sh` | Установка и управление WireGuard **сервером** |
| `wg-client.sh` | Установка WireGuard **клиента** — направляет весь трафик хоста через удалённый VPN |

---

## 🖥 wg-server.sh — WireGuard Сервер

Автоматическая установка WireGuard сервера без интерактивных вопросов.

### Быстрая установка

```bash
curl -sO https://raw.githubusercontent.com/torbikua/wireguard-installer/main/wg-server.sh && sudo bash wg-server.sh
```

### Возможности

- Установка в одну команду, без вопросов
- Автоопределение публичного IPv4/IPv6 и сетевого интерфейса
- Рандомный порт при каждой установке
- Создание клиентов с QR-кодом для мобильных устройств
- Меню управления на русском языке при повторном запуске

### Параметры по умолчанию

| Параметр | Значение |
|----------|----------|
| Интерфейс | `wg0` |
| Подсеть IPv4 | `10.10.10.1/24` |
| Подсеть IPv6 | `fd42:42:42::1/64` |
| DNS | `9.9.9.9`, `8.8.8.8` |
| Порт | рандомный (49152–65535) |

### Меню управления

При повторном запуске:

```
  1) Список клиентов
  2) Добавить нового клиента
  3) Отключить (удалить) клиента
  4) Удалить WireGuard сервер
  0) Выход
```

---

## 🔗 wg-client.sh — Исходящий WireGuard Клиент

Устанавливает WireGuard клиент на сервере и направляет **весь исходящий трафик** через удалённый WireGuard VPN. Реальный IP сервера скрывается, при этом SSH-доступ и входящие подключения продолжают работать.

### Быстрая установка

```bash
curl -sO https://raw.githubusercontent.com/torbikua/wireguard-installer/main/wg-client.sh && sudo bash wg-client.sh
```

### Возможности

- Установка на чистый сервер в одну команду
- Вставка любого WireGuard-конфига — скрипт сам его обработает
- Безопасное переключение маршрутов только после успешного handshake
- Защита SSH-сессии от разрыва при переключении
- Автоматический откат при неудаче
- Меню управления при повторном запуске

### Как это работает

```
Исходящий трафик ──► wg0 ──► Удалённый WG-сервер ──► Интернет
                                (новый внешний IP)

SSH ──► напрямую через реальный интерфейс (не через туннель)
```

- Весь исходящий трафик хоста идёт через WireGuard туннель
- Ответы на входящие SSH-подключения маркируются через `iptables mangle` и идут напрямую через реальный интерфейс
- Default route переключается на `wg0` только после успешного handshake
- DNS принудительно устанавливается на `1.1.1.1`, `8.8.8.8`, `9.9.9.9`

### Меню управления

При повторном запуске с активным туннелем:

```
  1) Показать статус
  2) Выключить туннель
  3) Перезапустить туннель
  4) Заменить конфиг VPN
  5) Удалить клиент полностью
  0) Выход
```

---

## 📋 Требования

- **ОС:** Debian 10+ / Ubuntu 18.04+
- **Права:** root
- **Ядро:** с поддержкой WireGuard (5.6+ встроен, для более ранних — dkms)

## ⚙️ Совместное использование

Типичный сценарий — сервер, трафик которого нужно пустить через удалённый VPN:

```
1. На удалённом сервере:  bash wg-server.sh   ← поднимает WG-сервер
2. На вашем сервере:      bash wg-client.sh   ← весь трафик через удалённый WG
```

Результат: ваш сервер выходит в интернет с IP удалённого WG-сервера, SSH-доступ сохраняется.

## 🛡 Безопасность

- SSH-сессия защищена от разрыва при переключении маршрутов
- Маршруты переключаются только после успешного WireGuard handshake
- При ошибке — автоматический откат к прямому подключению
- Приватные ключи хранятся с правами `600`

## 📄 Лицензия

MIT
